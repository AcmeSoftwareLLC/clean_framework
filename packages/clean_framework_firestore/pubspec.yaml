name: clean_framework_firestore
description: A wrapper around cloud_firestore to make it easier to use with clean_framework.
version: 0.5.2-dev.0
homepage: https://acmesoftware.com/
repository: https://github.com/AcmeSoftwareLLC/clean_framework/packages/clean_framework_firestore
# TODO(Chris): Remove when publishing.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  # clean_framework: ^4.0.0-dev.0
  clean_framework:
    git: 
      url: https://github.com/ChrisCostanzo-acme/clean_framework.git
      path: packages/clean_framework
      ref: chris/domain-refactors

  cloud_firestore: ^4.8.4
  equatable: ^2.0.5
  flutter:
    sdk: flutter

dev_dependencies:
  # clean_framework_test: ^0.5.3-dev.0
  clean_framework_test:
    git: 
      url: https://github.com/ChrisCostanzo-acme/clean_framework.git
      path: packages/clean_framework_test
      ref: chris/domain-refactors

  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^5.0.0+1
