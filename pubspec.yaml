name: feature_flag
description: A project to demostrate implementation of Feature Flags evaluation using Open Feature Specification.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=2.17.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # TODO: Add the "clean_framework" package as dependency

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/flags.json